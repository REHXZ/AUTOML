# Development — auth is disabled (no login gate)
# VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY are intentionally not set here.
# Set them only in your hosting platform's environment variables (Vercel / Render).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
